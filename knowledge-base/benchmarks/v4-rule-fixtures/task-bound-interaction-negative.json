{
  "version": "1.1",
  "contract": "task-bound-interaction-negative",
  "profiles": [{
    "id": "unbound-interaction",
    "name": "Unbound interaction",
    "environment": ["Deterministic repository fixture"],
    "journeys": [{
      "id": "unbound-task",
      "name": "unbound-task",
      "steps": [{ "action": "expectVisible", "selector": "main" }]
    }]
  }]
}
