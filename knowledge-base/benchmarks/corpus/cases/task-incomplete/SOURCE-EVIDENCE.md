# Synthetic Source Evidence

This deliberately incomplete contract is project-owned test data. It contains structurally valid declarations with broken cross-references so the corpus can verify deterministic rejection.
